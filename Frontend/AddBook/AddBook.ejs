<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Book - Library Management System</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <script src="https://kit.fontawesome.com/1b2855684f.js" crossorigin="anonymous"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="flex justify-between p-4 items-center sticky top-0 bg-white z-50 shadow-sm">
        <div>
            <h1 class="text-2xl font-bold text-gray-800">Add New Book</h1>
        </div>
        <div class="flex gap-4 items-center">
            <a href="/render/home" class="text-gray-600 hover:text-gray-800 transition duration-200">
                <span class="font-semibold">Home</span>
            </a>
            <a href="/render/library" class="text-gray-600 hover:text-gray-800 transition duration-200">
                <span class="font-semibold">Library</span>
            </a>
        </div>
    </div>

    <div class="max-w-md mx-auto p-6">
        <div class="bg-white p-8 rounded-xl shadow-lg">
            <div class="text-center mb-8">
                <h1 class="text-3xl font-bold text-gray-800">Add New Book</h1>
                <p class="text-gray-600 mt-2">Enter book details below</p>
            </div>
            <form action="/book/" method="POST" class="space-y-6">
                <div>
                    <label for="title" class="block text-sm font-medium text-gray-700">Title</label>
                    <div class="mt-1 relative">
                        <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">
                            <i class="fas fa-book"></i>
                        </span>
                        <input 
                            type="text" 
                            id="title" 
                            name="title" 
                            required
                            placeholder="Enter book title"
                            class="pl-10 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:ring-2 focus:ring-black focus:border-black focus:outline-none transition duration-200"
                        >
                    </div>
                </div>

                <div>
                    <label for="author" class="block text-sm font-medium text-gray-700">Author</label>
                    <div class="mt-1 relative">
                        <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">
                            <i class="fas fa-user-edit"></i>
                        </span>
                        <input 
                            type="text" 
                            id="author" 
                            name="author" 
                            required
                            placeholder="Enter author name"
                            class="pl-10 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:ring-2 focus:ring-black focus:border-black focus:outline-none transition duration-200"
                        >
                    </div>
                </div>

                <div>
                    <label for="description" class="block text-sm font-medium text-gray-700">Description</label>
                    <div class="mt-1 relative">
                        <span class="absolute top-3 left-0 pl-3 flex items-center text-gray-500">
                            <i class="fas fa-align-left"></i>
                        </span>
                        <textarea
                            id="description"
                            name="description"
                            rows="4"
                            required
                            placeholder="Enter book description"
                            class="pl-10 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:ring-2 focus:ring-black focus:border-black focus:outline-none transition duration-200"
                        ></textarea>
                    </div>
                </div>

                <div>
                    <label for="genre" class="block text-sm font-medium text-gray-700">Genre (comma separated)</label>
                    <div class="mt-1 relative">
                        <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">
                            <i class="fas fa-tags"></i>
                        </span>
                        <input 
                            type="text" 
                            id="genre" 
                            name="genre" 
                            required
                            placeholder="Fiction, Fantasy, Adventure"
                            class="pl-10 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:ring-2 focus:ring-black focus:border-black focus:outline-none transition duration-200"
                        >
                    </div>
                </div>

                <div>
                    <label for="copiesAvailable" class="block text-sm font-medium text-gray-700">Number of Copies</label>
                    <div class="mt-1 relative">
                        <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">
                            <i class="fas fa-copy"></i>
                        </span>
                        <input 
                            type="number" 
                            id="copiesAvailable" 
                            name="copiesAvailable" 
                            required
                            min="1"
                            placeholder="Enter number of copies"
                            class="pl-10 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:ring-2 focus:ring-black focus:border-black focus:outline-none transition duration-200"
                        >
                    </div>
                </div>

                <div>
                    <label for="publishedYear" class="block text-sm font-medium text-gray-700">Published Year</label>
                    <div class="mt-1 relative">
                        <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">
                            <i class="fas fa-calendar"></i>
                        </span>
                        <input 
                            type="number" 
                            id="publishedYear" 
                            name="publishedYear" 
                            required
                            min="1000"
                            placeholder="Enter published year"
                            class="pl-10 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:ring-2 focus:ring-black focus:border-black focus:outline-none transition duration-200"
                        >
                    </div>
                </div>

                <button 
                    type="submit" 
                    class="w-full bg-black text-white py-3 px-4 rounded-lg hover:bg-gray-800 transform hover:scale-[1.02] transition-all duration-200 font-medium"
                >
                    Add Book
                </button>
            </form>
        </div>
    </div>
</body>
</html>
