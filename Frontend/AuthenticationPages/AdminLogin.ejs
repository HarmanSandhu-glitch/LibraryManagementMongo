<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - Library Management System</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <script src="https://kit.fontawesome.com/1b2855684f.js" crossorigin="anonymous"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
    <div class="bg-white p-8 rounded-xl shadow-lg w-full max-w-md">
        <div class="text-center mb-8">
            <h1 class="text-3xl font-bold text-gray-800">Admin Login</h1>
            <p class="text-gray-600 mt-2">Welcome back! Please login to your account.</p>
        </div>
        <form action="/auth/admin/login" method="POST" class="space-y-6">
            <div>
                <label for="email" class="block text-sm font-medium text-gray-700">Email Address</label>
                <div class="mt-1 relative">
                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">
                        <i class="fas fa-envelope"></i>
                    </span>
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        required
                        placeholder="Enter your email"
                        class="pl-10 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:ring-2 focus:ring-black focus:border-black focus:outline-none transition duration-200"
                    >
                </div>
            </div>
            <div>
                <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
                <div class="mt-1 relative">
                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">
                        <i class="fas fa-lock"></i>
                    </span>
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        required
                        placeholder="Enter your password"
                        class="pl-10 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:ring-2 focus:ring-black focus:border-black focus:outline-none transition duration-200"
                    >
                </div>
            </div>
            <button 
                type="submit" 
                class="w-full bg-black text-white py-3 px-4 rounded-lg hover:bg-gray-800 transform hover:scale-[1.02] transition-all duration-200 font-medium"
            >
                Sign In
            </button>
        </form>
        <div class="mt-8 text-center space-y-3">
            <div class="relative">
                <div class="absolute inset-0 flex items-center">
                    <div class="w-full border-t border-gray-300"></div>
                </div>
                <div class="relative flex justify-center">
                    <span class="px-2 bg-white text-sm text-gray-500">Or</span>
                </div>
            </div>
            <a href="/render/auth/admin/register" class="block text-gray-600 hover:text-gray-800 transition duration-200">
                Don't have an account? 
                <span class="font-semibold text-black hover:underline">Register here</span>
            </a>
            <a href="/render/auth/student/login" class="block text-gray-600 hover:text-gray-800 transition duration-200">
                Are you a student? 
                <span class="font-semibold text-black hover:underline">Login here</span>
            </a>
        </div>
    </div>
</body>
</html>
